# Gemini API Configuration
GEMINI_API_KEY=AIzaSyDEOIqj_9VnnwBevRVpc6ZdECIASu_8BWI
GEMINI_MODEL=gemini-2.5-flash

# Rate Limiting Configuration (IMPORTANT for free tier)
GEMINI_REQUEST_DELAY=2.0  # Seconds to wait between requests (increase if hitting limits)
MAX_RETRIES=5  # Maximum retry attempts for failed requests
RETRY_DELAY=60  # Initial retry delay in seconds (will increase exponentially)
EXPONENTIAL_BACKOFF=true  # Use exponential backoff for retries

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# File Upload Configuration
MAX_UPLOAD_SIZE=52428800  # 50MB in bytes
UPLOAD_DIR=uploads

# Logging Configuration
LOG_DIR=logs
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Model Configuration
TABLE_DETECTION_MODEL=microsoft/table-transformer-detection
TABLE_CONFIDENCE_THRESHOLD=0.7

# Processing Configuration
DEFAULT_CHUNK_SIZE=5
DEFAULT_OVERLAP=1